# Configuration du serveur
NODE_ENV=production
PORT=6610

# Configuration de la base de données MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=lounge_user
DB_PASSWORD=changeMe!
DB_DATABASE=lounge_db

# JWT pour l'authentification
JWT_SECRET=changeThis-WithSecureSecret
JWT_EXPIRATION=7d

# Configuration des uploads
UPLOADS_DIR=./uploads

# URL de base de l'API (pour les URLs absolues dans les réponses)
API_BASE_URL=https://api.votredomaine.com

# Autres configurations (optionnelles)
CORS_ORIGINS=https://votredomaine.com,http://localhost:3000
RATE_LIMIT_WINDOW=15m
RATE_LIMIT_MAX=100
LOG_LEVEL=info 